name: Develop

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x]

    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: npm install, build, and test
      run: |
        npm install
        npm run build
        npm test
      env:
        CI: true
    - name: performance
      run: |
        npm install
        npm run build
        cd performance
        npm install
        npm start $JSON_BIN_PERFORMANCE
      env:
        JSON_BIN_PERFORMANCE: ${{ secrets.JSON_BIN_PERFORMANCE }}

